name: incus-agent
container:
  entrypoint: ./incus-agent-wrapper
  security:
    writeableRootfs: true
  mounts:
    - source: /dev
      destination: /dev
      type: bind
      options:
        - rshared
        - rbind
        - rw
    - source: /sys
      destination: /sys
      type: bind
      options:
        - ro
        - rbind
    - source: /proc
      destination: /proc
      type: bind
      options:
        - ro
        - rbind
restart: always
logToConsole: true
