name: incus-agent
container:
  entrypoint: ./incus-agent-wrapper
  security:
    writeableRootfs: true
  mounts:
    - source: /dev
      destination: /dev
      type: bind
      options:
        - rshared
        - rbind
        - rw
restart: always
